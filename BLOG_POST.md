# [개발기] API 키 없이 만든 게임업계 채용공고 적합도 분석 AI 에이전트

게임 개발자 취업을 준비하다 보면 공고마다 기술 스택이 달라 내가 얼마나 적합한지 판단하기 어렵습니다. 그래서 채용공고와 내 경험을 입력하면 적합도와 부족한 역량을 알려주는 **GameFit Agent**를 만들었습니다.

## 무엇을 만들었나

GameFit Agent는 네 가지 도구를 상황에 맞게 선택해 실행합니다.

1. 채용공고 요구사항 추출
2. 지원자 보유 기술 분석
3. 기술 적합도 계산
4. 부족 역량 개선 계획 생성

에이전트는 각 도구의 출력을 다음 도구에 전달합니다. 최종 화면에는 점수, 일치/부족 기술, 개선 계획과 함께 어떤 도구가 왜 선택됐는지도 보여줍니다.

## 왜 규칙 기반으로 만들었나

과제를 다른 사람도 쉽게 실행할 수 있게 만드는 것이 중요했습니다. 외부 생성형 AI API는 키 발급, 비용, 네트워크 상태에 따라 재현이 어려울 수 있습니다. 그래서 Python 표준 라이브러리와 설명 가능한 규칙 기반 라우터를 선택했습니다. 생성형 AI를 쓰지 않더라도 목표를 세우고 여러 도구를 선택·연결한다는 에이전트 구조는 코드에서 분명히 확인할 수 있습니다.

## 핵심 구현

`@tool` 데코레이터로 도구를 중앙 등록소에 넣습니다. 에이전트는 입력 상태를 보고 도구 목록을 선택한 다음 등록소를 통해 호출합니다. 각 호출 이유는 실행 추적에 저장됩니다. 덕분에 새 분석 기능도 도구 하나를 등록하는 방식으로 확장할 수 있습니다.

서버는 Python의 `ThreadingHTTPServer`를 사용했고 화면은 순수 HTML/CSS/JavaScript로 만들었습니다. 외부 의존성이 없어 실행 명령은 `python run.py` 하나뿐입니다.

## 만들며 해결한 문제

처음에는 단순 문자열 검색 때문에 `go`처럼 짧은 기술명이 다른 단어에서도 잡힐 수 있었습니다. 정규식 단어 경계를 적용해 오탐을 줄였습니다. 기술이 하나도 추출되지 않았을 때 점수 계산에서 0으로 나누는 문제도 별도 분기로 처리했습니다. 한글 JSON은 UTF-8과 `ensure_ascii=False` 설정으로 해결했습니다.

## 사용 후기와 다음 계획

실제로 제 프로젝트 경험을 넣어 보니 무엇을 더 공부할지 빠르게 정리되는 점이 유용했습니다. 반면 키워드 사전에 없는 표현이나 경력의 깊이는 이해하지 못한다는 한계가 있었습니다. 앞으로 로컬 LLM 선택 옵션, 필수/우대 가중치, 여러 공고 비교와 PDF 내보내기를 추가하고 싶습니다.

- GitHub: **여기에 본인 저장소 링크를 입력하세요**
- 실행 방법과 전체 코드는 저장소 README에서 확인할 수 있습니다.

