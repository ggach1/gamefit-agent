# GameFit Agent 개발일지

## 1. 기획

게임회사 공고는 요구 기술이 길게 나열되어 있어 취업 준비생이 “내가 무엇을 더 준비해야 하는지” 빠르게 판단하기 어렵습니다. 이를 해결하기 위해 공고와 지원자 경험을 구조화하고 비교하는 에이전트를 기획했습니다.

설계 목표는 다음 세 가지였습니다.

1. API 키 없이 누구나 바로 실행할 수 있을 것
2. 에이전트가 어떤 도구를 왜 썼는지 확인할 수 있을 것
3. 분석에서 끝나지 않고 구체적인 다음 행동을 제안할 것

## 2. 개발 방법

Python 표준 라이브러리의 `ThreadingHTTPServer`로 서버를 만들고 HTML/CSS/JavaScript로 웹 화면을 구현했습니다. 외부 프레임워크가 없기 때문에 설치 오류가 적고 학교 PC에서도 실행하기 쉽습니다.

도구는 데코레이터 패턴으로 등록했습니다. `@tool(name, description)`이 함수 정보를 중앙 `REGISTRY`에 넣고, 에이전트의 `_invoke()`가 이름으로 도구를 실행합니다. 도구 추가 시 에이전트 서버 코드를 크게 고치지 않아도 됩니다.

## 3. 구현 순서

- 기술 키워드 사전과 정규식 기반 추출기 작성
- 공고 분석, 지원자 분석, 적합도 계산, 개선 계획 생성 도구 구현
- 입력 상태와 중간 결과에 따른 도구 선택 로직 구현
- `/api/health`, `/api/tools`, `/api/analyze` API 구현
- 분석 결과와 실행 추적을 보여주는 반응형 웹 UI 구현
- `unittest` 기반 자동 테스트 작성

## 4. 코드 설명

### `tools.py`

도구 등록 데코레이터와 4개 도구가 있습니다. 기술명은 소문자로 정규화하고 정규식의 단어 경계를 이용해 `go`, `db` 같은 짧은 단어의 오탐을 줄였습니다.

### `agent.py`

`select_tools()`는 입력 유무로 사용할 도구를 결정합니다. `analyze()`는 앞 도구의 출력을 다음 도구의 입력으로 전달하고 각 선택 이유를 `trace`에 남깁니다. 이 부분이 일반 키워드 검색기와 에이전트 서버를 구분하는 핵심입니다.

### `server.py`

정적 웹 파일과 JSON API를 함께 제공합니다. 잘못된 입력은 400 응답으로 반환하고 200KB가 넘는 요청은 거부합니다. 파일 경로를 `resolve()`로 검사해 웹 루트 밖의 파일 접근도 막았습니다.

### `web/`

샘플 공고와 프로필을 기본 제공하며, 분석 버튼을 누르면 비동기로 API를 호출합니다. 결과에는 점수뿐 아니라 실제로 선택된 도구와 이유도 표시됩니다.

## 5. 트러블슈팅

### 짧은 기술명이 일반 단어 안에서 검출되는 문제

초기에는 문자열 포함 여부만 검사해 `go`가 다른 영단어 일부에서도 검출될 수 있었습니다. 영문·숫자 기준 앞뒤 경계를 확인하는 정규식으로 해결했습니다.

### 요구 기술이 없는 경우 0으로 나누는 문제

추출된 공고 기술이 없으면 분모가 0이 됩니다. 이때 점수를 0으로 처리하도록 분기했습니다.

### 웹 경로를 이용한 상위 폴더 접근 가능성

요청 경로를 그대로 파일 경로로 쓰지 않고 절대경로로 변환한 뒤 반드시 `web` 폴더 아래인지 확인했습니다.

### 한글 JSON이 이스케이프되는 문제

`json.dumps(..., ensure_ascii=False)`와 UTF-8 응답 헤더를 사용해 한글이 그대로 보이도록 했습니다.

## 6. 검증 방법

`python -m unittest discover -s tests -v`로 4개 테스트를 실행합니다. 또한 브라우저에서 샘플 분석을 실행해 화면과 API 응답을 직접 확인합니다.

## 7. 실행 화면

최종 실행 캡처는 `docs/screenshots/gamefit-analysis.jpg`에 저장합니다.
같은 상태를 다시 캡처하려면 서버 실행 후 `http://127.0.0.1:8000/?demo=1`을 열면 샘플 분석이 자동 실행됩니다.
